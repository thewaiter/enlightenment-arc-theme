text_classes {
#define INHERIT_DEFAULT(_name, _size) \
    text_class { \
        name: _name; \
        font: FN; \
        size: _size; \
    }
    INHERIT_DEFAULT("check", FNSIZE)
    INHERIT_DEFAULT("check_button", FNSIZE)
    INHERIT_DEFAULT("check_text_disabled", FNSIZE)
    INHERIT_DEFAULT("radio", FNSIZE)
    INHERIT_DEFAULT("frame", FNSIZE)
    INHERIT_DEFAULT("list_item", FNSIZE);
    INHERIT_DEFAULT("popup_text", FNSIZE);
    INHERIT_DEFAULT("slider_text", FNSIZE);
}